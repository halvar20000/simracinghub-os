#!/usr/bin/bash
#
# SimRacing Hub OS — SimHub launcher
#
# STATUS: placeholder scaffold (Phase 2).
# The working version arrives in Phase 4.
#
# Planned behaviour:
#   Launch SimHub from the dedicated Wine prefix created by simhub-setup.sh,
#   i.e. run SimHubWPF.exe inside:
#     ~/.local/share/simracinghub-os/simhub-prefix

set -euo pipefail

echo "SimRacing Hub OS — SimHub launcher"
echo
echo "This launcher is not active yet — it is enabled in a later build (Phase 4)."
echo "Run the SimHub setup helper first once it is available."
exit 0
